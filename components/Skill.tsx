import React, { useRef } from "react";

interface SkillProps {}

const Skill: React.FC<SkillProps> = ({}) => {
  return (
    <section className="bg-slate-100">
      <main className="min-h-screen px-6 py-10 max-w-5xl m-auto">
        <h1>skill</h1>
      </main>
    </section>
  );
};

export default Skill;
